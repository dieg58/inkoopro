{
  "buildCommand": "npx prisma generate && npx prisma db push --skip-generate --accept-data-loss && npm run build",
  "installCommand": "npm install",
  "framework": "nextjs",
  "regions": ["cdg1"]
}

