{
  "buildCommand": "npx prisma generate && npx tsx scripts/clean-failed-migrations.ts || true && npx prisma migrate deploy && npm run build",
  "installCommand": "npm install",
  "framework": "nextjs",
  "regions": ["cdg1"]
}

