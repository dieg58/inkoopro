{
  "buildCommand": "npm run setup:prod && npx prisma db push --skip-generate --accept-data-loss || echo 'Database push skipped or failed, continuing build...' && npm run build",
  "installCommand": "npm install",
  "framework": "nextjs",
  "regions": ["cdg1"]
}

