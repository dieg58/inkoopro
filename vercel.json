{
  "buildCommand": "npm run setup:prod && npx prisma generate && (npx prisma migrate deploy || npx prisma db push --accept-data-loss || echo 'Database sync skipped, continuing build...') && npm run build",
  "installCommand": "npm install",
  "framework": "nextjs",
  "regions": ["cdg1"],
  "crons": [
    {
      "path": "/api/cron/sync-products",
      "schedule": "0 */6 * * *"
    }
  ]
}

